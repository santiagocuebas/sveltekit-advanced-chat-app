export enum TypeContact {
  USER = 'user',
  GROUP = 'group'
}

export enum StateOption {
  PUBLIC = 'public',
  PROTECTED = 'protected',
  PRIVATE = 'private'
}

export enum Ext {
  PNG = '.png',
  JPG = '.jpg',
  JPEG = '.jpeg',
  GIF = '.gif'
}
